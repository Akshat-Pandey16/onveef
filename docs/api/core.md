# Sans-IO core

Neither module touches the network: `envelopes` turns arguments into request strings and
`parsers` turns response strings into dicts. Both are pure functions, which is why device
behaviour can be verified from recorded XML.

## Request builders

::: onveef.envelopes

## Response parsers

::: onveef.parsers

## Physical Access Control (Profile A/C)

::: onveef.pacs
